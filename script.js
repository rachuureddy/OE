const uri = await pdfDoc.saveAsBase({dataUri: true });
saveAs(uri,"course certificate.pdf",{autobom:true});
}:
const submitbtn=documnemt .getElementById ("submit")
const inputVal=document.querySelector("#name");
submitbtn.addEventListener("click",())
{
    const val = inputVal.ariaValueMax;
    generatorPDF(val);
}):
}